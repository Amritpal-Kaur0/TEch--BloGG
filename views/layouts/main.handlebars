<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    {{!-- Render the  nav bar --}}
    <header class="bg-primary display-flex justify-space-between align-center p-5">
      <h1>
        <a href="/"><span>T</span>ech-Blog</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->   
        <button class="btn btn-light" id="home">
          <a href="/">Home</a>
        </button>
        
        {{#if logged_in}}
        <button class="btn btn-light" id="Dashboard">
          <a href="/dashboard">Dashboard</a>
        </button>

        <button class="btn btn-light" id="logout">
          Logout
        </button>

        {{else}}
        <button class="btn btn-light" id="login">
          <a href="/login">Login</a>
        </button>
        {{/if}}

      </nav>
    </header>

    <main class="container container-fluid mt-5">
     
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>